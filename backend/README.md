# Backend (Flask)

## Local startup

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python run.py
```

## Run tests

```powershell
cd backend
.\.venv\Scripts\Activate.ps1
pytest -q
```
